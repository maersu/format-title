# Agent Instructions

- When making changes to this extension, increment the `version` field in `manifest.json`.
- Keep changes small and focused on the requested behavior.
- Validate `manifest.json` after editing it.
